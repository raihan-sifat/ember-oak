# Used only for production builds — points the client at the deployed backend.
# Leave this unset for local dev; Vite's proxy (vite.config.js) handles /api locally.
VITE_API_URL=https://your-backend.onrender.com
